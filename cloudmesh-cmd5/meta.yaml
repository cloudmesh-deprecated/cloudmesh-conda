package:
  name: cloudmesh-cmd5
  version: "{{ load_setup_py_data().version }}"

source:
  git_url: https://github.com/cloudmesh/cloudmesh-cmd5.git

build:
  number: 0

requirements:
  build:
    - python
    - pyyaml
    - docopt
    - requests

  run:
    - pyyaml
    - cloudmesh-common
    - docopt
    - requests



